import { createStyles } from "@/lib/mantine/core";

export default createStyles(() => ({
  preview_container: {
    width: 600,
    height: 400,
    backgroundColor: "black",
    position: "relative",
  },
}));
